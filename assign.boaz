program example
int num, sum;
char ch;
begin
    sum := 0;
    num := 1;
    ch := "a";
    sum := sum + num * 2 / 4;
    ch := ch + 1;
    sum := sum ------ num;
    ch := "m";
    if num/3!=0&ch!="y"then ch:=ch+1;fi;
end